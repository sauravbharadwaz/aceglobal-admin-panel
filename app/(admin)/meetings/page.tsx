import { ComingSoon } from "@/components/coming-soon";

export default function MeetingsPage() {
  return (
    <ComingSoon
      title="Meetings"
      description="Scheduled calls and consultations with clients."
      note="Meetings will be a manual table first (log calls, times, and outcomes), with an optional calendar integration (Cal.com / Google) in a later phase."
    />
  );
}
