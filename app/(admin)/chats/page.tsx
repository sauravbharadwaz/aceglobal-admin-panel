import { ComingSoon } from "@/components/coming-soon";

export default function ChatsPage() {
  return (
    <ComingSoon
      title="Chats"
      description="Client conversations across WhatsApp and iMessage."
      note="Messaging is the largest piece — it will likely embed your existing chat provider or be built as a dedicated phase."
    />
  );
}
